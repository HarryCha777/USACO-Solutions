#include <iostream>
#include <fstream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

int main()
{
	ifstream in("perimeter.in");
	ofstream out("perimeter.out");

	int N;
	in >> N;

	int hays[60000] = {};

	for (int i = 0; i < N; i++)
	{
		in >> hays[i];
	}

	int pm = 0;
	
	x x x x
	x x     x x x x
	x x x x x x

	cout << pm << endl;
	cin.get();

    return 0;
}

